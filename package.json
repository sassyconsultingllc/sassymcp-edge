{
  "name": "sassymcp-edge",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dev:personal": "wrangler dev --config wrangler.personal.jsonc",
    "deploy:personal": "wrangler deploy --config wrangler.personal.jsonc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260610.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.99.0"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.7.2",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.15.0",
    "zod": "^4.4.3"
  }
}
